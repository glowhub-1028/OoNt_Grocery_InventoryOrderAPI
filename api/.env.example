# Copy to .env and fill in your values.
# For local Docker Postgres, use the same credentials as in docker-compose.yml.

DATABASE_URL="postgresql://grocery_user:grocery_password@localhost:5432/grocery_db"
